## for circleCI configuration file
machine:
  timezone:
    Asia/Tokyo
  java:
    version: oraclejdk8
test:
  override:
    - mvn clean test
deployment:
  feature:
    branch: /feature.*/
    commands:
      - sh ./sh/deploy_feature.sh
