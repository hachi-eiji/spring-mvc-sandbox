## for circleCI configuration file
machine:
  timezone:
    Asia/Tokyo
  java:
    version: oraclejdk8
test:
  override:
    - mvn clean test
development:
  dev:
    branch: feauture/*
    command:
      - ./sh/deploy_feature.sh
