## for circleCI configuration file
machine:
  timezone:
    Asia/Tokyo
  java:
    version: oraclejdk8
test:
  override:
    - mvn clean test
deployment:
  dev:
    branch: feature/*
    command:
      - ./sh/deploy_feature.sh
